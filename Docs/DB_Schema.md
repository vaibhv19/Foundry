# DB_Schema.md — Foundry Database Architecture

This document defines the relational data model for **Foundry**. The schema is optimized to support multi-agent state persistence, granular version control for blueprint sections, and the high-precision retrieval required by the **Decision Memory & Consistency Engine**.

---

## 1. Entity Relationship Diagram

```text
                     +-------+
                     | users |
                     +-------+
                         | 1
                         |
           +-------------+-------------+
           | 1                         | 1
     +-----v-----+               +-----v------+
     |   ideas   |               | blueprints |
     +-----┬-----+               +-----┬------+
           | 1                         | 1
           |             +-------------+-------------+
           +-------------|-------------+             |
                         | M                         | M
                  +------v-------+            +------v-------+
                  |   sections   |            | decision_log |<--+
                  +------┬-------+            +------┬-------+   | (supersedes_id)
                         | 1                         |           |
                         | M                         |           +---+
                  +------v-------+                   |
                  |   versions   |<------------------+
                  +------┬-------+ (created_by_version_id)
                         | 1
                  +------v-------+
                  |     jobs     |
                  +--------------+
```

---

## 2. Table Dictionary

1.  **`users`**: Core identity and authentication table.
2.  **`ideas`**: Immutable storage of the original one-paragraph startup concepts submitted by users.
3.  **`blueprints`**: The primary container for a startup plan; tracks overall generation status and ownership.
4.  **`sections`**: Logical divisions of a blueprint (e.g., Market Positioning, Technical Architecture).
5.  **`versions`**: Incremental snapshots of section content; every AI regeneration or manual edit creates a new record here.
6.  **`decision_log`**: The **Consistency Engine's** source of truth; stores normalized, atomic choices made by agents.
7.  **`jobs`**: Orchestration table for Celery/LangGraph tasks; tracks real-time progress and logs errors for the UI.

---

## 3. Table Specifications

### 3.1 `users`
| Column | Type | Constraints |
| :--- | :--- | :--- |
| `id` | `UUID` | PK, Default: gen_random_uuid() |
| `email` | `VARCHAR(255)` | Unique, Not Null |
| `password_hash` | `VARCHAR(255)` | Not Null |
| `name` | `VARCHAR(100)` | Not Null |
| `created_at` | `TIMESTAMPTZ` | Default: NOW() |

### 3.2 `ideas`
| Column | Type | Constraints |
| :--- | :--- | :--- |
| `id` | `UUID` | PK |
| `user_id` | `UUID` | FK (users.id) |
| `raw_text` | `TEXT` | Not Null (The original input) |
| `created_at` | `TIMESTAMPTZ` | Default: NOW() |

### 3.3 `blueprints`
| Column | Type | Constraints |
| :--- | :--- | :--- |
| `id` | `UUID` | PK |
| `user_id` | `UUID` | FK (users.id) |
| `idea_id` | `UUID` | FK (ideas.id) |
| `title` | `VARCHAR(255)` | Not Null (Auto-generated by Investor) |
| `status` | `ENUM` | `QUEUED`, `GENERATING`, `READY`, `FAILED` |
| `created_at` | `TIMESTAMPTZ` | Default: NOW() |

### 3.4 `sections`
| Column | Type | Constraints |
| :--- | :--- | :--- |
| `id` | `UUID` | PK |
| `blueprint_id` | `UUID` | FK (blueprints.id) |
| `category` | `ENUM` | `MARKET`, `PRODUCT`, `TECH_STACK`, `BUSINESS` |
| `sort_order` | `INT` | For UI rendering sequence |

### 3.5 `versions`
| Column | Type | Constraints |
| :--- | :--- | :--- |
| `id` | `UUID` | PK |
| `section_id` | `UUID` | FK (sections.id) |
| `version_number` | `INT` | Auto-increment per section |
| `content_markdown` | `TEXT` | The prose generated by agents |
| `is_active` | `BOOLEAN` | Default: FALSE (The version shown on Canvas) |
| `created_at` | `TIMESTAMPTZ` | Default: NOW() |

### 3.6 `decision_log` (The Consistency Engine Core)
This table is queried whenever a section is regenerated to prevent logical drift.
| Column | Type | Constraints |
| :--- | :--- | :--- |
| `id` | `UUID` | PK |
| `blueprint_id` | `UUID` | FK (blueprints.id) |
| `created_by_version_id`| `UUID` | FK (versions.id) - Which gen attempt made this? |
| `node_origin` | `ENUM` | `INVESTOR`, `PM`, `TECH_LEAD` |
| `decision_key` | `VARCHAR(100)`| Slug: e.g., `primary_database` |
| `choice_value` | `TEXT` | e.g., `PostgreSQL` |
| `rationale` | `TEXT` | Extracted reasoning for the Decision Memory UI |
| `supersedes_id` | `UUID` | FK (decision_log.id) - Self-reference for overrides |
| `is_active` | `BOOLEAN` | If FALSE, this decision was overridden by user |

### 3.7 `jobs`
| Column | Type | Constraints |
| :--- | :--- | :--- |
| `id` | `UUID` | PK (Matches Celery `task_id`) |
| `blueprint_id` | `UUID` | FK (blueprints.id) |
| `target_section_id` | `UUID` | FK (sections.id), Nullable if initial gen |
| `task_type` | `ENUM` | `INITIAL_DEBATE`, `SECTION_REGEN` |
| `status` | `ENUM` | `PENDING`, `STARTED`, `SUCCESS`, `FAILURE` |
| `current_node` | `VARCHAR(50)` | e.g., `Investor` (Updates Channels UI) |
| `error_log` | `TEXT` | Traceback if status is FAILURE |

---

## 3.8 Extended Runtime and Lifecycle Tables

The initial schema is sufficient for basic generation, but the production-grade version needs a small number of explicit runtime tables to support debate state, exports, and attachments.

### 3.8.1 `agent_runs`
| Column | Type | Constraints |
| :--- | :--- | :--- |
| `id` | `UUID` | PK |
| `blueprint_id` | `UUID` | FK (blueprints.id) |
| `run_type` | `ENUM` | `INITIAL_DEBATE`, `REGENERATION`, `MANUAL_OVERRIDE` |
| `status` | `ENUM` | `PENDING`, `RUNNING`, `COMPLETED`, `FAILED` |
| `started_at` | `TIMESTAMPTZ` | Default: NOW() |
| `finished_at` | `TIMESTAMPTZ` | Nullable |

### 3.8.2 `agent_messages`
| Column | Type | Constraints |
| :--- | :--- | :--- |
| `id` | `UUID` | PK |
| `agent_run_id` | `UUID` | FK (agent_runs.id) |
| `agent_name` | `VARCHAR(50)` | e.g. `Investor`, `Product_Manager`, `Tech_Lead` |
| `message_type` | `VARCHAR(20)` | e.g. `TOKEN`, `STATUS`, `DEBUG` |
| `content` | `TEXT` | Payload or stream chunk |
| `created_at` | `TIMESTAMPTZ` | Default: NOW() |

### 3.8.3 `generation_events`
| Column | Type | Constraints |
| :--- | :--- | :--- |
| `id` | `UUID` | PK |
| `blueprint_id` | `UUID` | FK (blueprints.id) |
| `event_type` | `VARCHAR(50)` | `JOB_CREATED`, `NODE_STARTED`, `NODE_COMPLETED`, `NODE_FAILED`, `COMPLETE`, `ERROR` |
| `details` | `JSONB` | Event payload |
| `created_at` | `TIMESTAMPTZ` | Default: NOW() |

### 3.8.4 `exports`
| Column | Type | Constraints |
| :--- | :--- | :--- |
| `id` | `UUID` | PK |
| `blueprint_id` | `UUID` | FK (blueprints.id) |
| `format` | `VARCHAR(20)` | `MARKDOWN`, `PDF`, `DOCX` |
| `storage_path` | `TEXT` | File location |
| `created_at` | `TIMESTAMPTZ` | Default: NOW() |

### 3.8.5 `attachments`
| Column | Type | Constraints |
| :--- | :--- | :--- |
| `id` | `UUID` | PK |
| `blueprint_id` | `UUID` | FK (blueprints.id) |
| `file_name` | `VARCHAR(255)` | Not Null |
| `mime_type` | `VARCHAR(100)` | Not Null |
| `storage_path` | `TEXT` | Not Null |
| `created_at` | `TIMESTAMPTZ` | Default: NOW() |

### 3.8.6 Suggested Indexes
- `decision_log(blueprint_id, is_active)`
- `versions(section_id, version_number)`
- `agent_runs(blueprint_id, status)`
- `generation_events(blueprint_id, created_at)`
- `exports(blueprint_id, created_at)`

### 3.8.7 Relationship Notes
- `blueprints` has one-to-many relationships to `agent_runs`, `generation_events`, `exports`, and `attachments`.
- `agent_runs` has one-to-many relationships to `agent_messages`.
- `versions` remain the authoritative content snapshots for section regeneration and rollback.

## 4. Key Retrieval Logic: The "Consistency Join"

When a user requests a regeneration for a **Tech Stack** section (Flow 3), the backend executes the following logic:

1.  **Identity:** Locate the `blueprint_id` for the target section.
2.  **Retrieve Commitments:** 
    ```sql
    SELECT decision_key, choice_value, rationale 
    FROM decision_log 
    WHERE blueprint_id = :id AND is_active = TRUE;
    ```
3.  **Inject:** These key-value pairs are injected into the LangGraph state before the agent node executes.
4.  **Update:** If the agent node proposes a new decision (User Override), the system inserts a new `decision_log` row and updates the `supersedes_id` of the previous active row with the same `decision_key`.